---
layout: archive
title: <%= tag %>
---

<ul class="tag-list">
    <li>Posts tagged as '<%= tag %>':</li>
    <ul>
        <% posts.each do |post| %>
            <li>&rarr; <a href="<%= post.url %>"><%= post.data['title'] %></a></li>
        <% end %>
    </ul>
</ul>
